import { SearchTimeDirective } from './search-time.directive';

describe('SearchTimeDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchTimeDirective();
    expect(directive).toBeTruthy();
  });
});
